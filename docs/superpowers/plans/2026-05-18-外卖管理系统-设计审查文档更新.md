# 外卖管理系统设计审查 — 文档更新实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 根据设计审查结果，更新三份核心文档（系统需求分析、概念逻辑结构设计、项目架构文档）并重新生成所有图表。

**Architecture:** 按文档逐一更新。先改需求分析（上层），再改概念逻辑设计（中间层 E-R 图），再改项目架构文档（底层 SQL + API），最后重新生成 diagrams/ 下的所有图片。每份文档改完后提交一次 commit。

**Tech Stack:** Markdown（文档格式）、Mermaid（E-R 图/流程图）、mmdc CLI（图表生成）、Python（drawio 生成脚本）

---

### Task 1: 更新系统需求分析 — 功能描述（§1-§3.1）

**Files:**
- Modify: `外卖管理系统-系统需求分析.md`

- [ ] **Step 1: 更新 §1 系统介绍 — 补充技术方案说明**

在 §1 第四段（"系统核心功能包括..."）末尾，补充技术方案说明。

定位原文：
```
系统核心功能包括：用户管理、商家管理、菜品管理、订单管理、配送管理、查询统计。支付与评价功能暂不在本期范围内，但系统在订单和商家数据结构上预留了扩展字段，便于后续接入。
```

替换为：
```
系统核心功能包括：用户管理、商家管理、菜品管理、订单管理、配送管理、查询统计。支付与评价功能暂不在本期范围内，但系统在订单和商家数据结构上预留了扩展字段，便于后续接入。

系统采用 B/S 架构，后端 Java Spring Boot + MySQL + Redis，前端 Vue 3 + Element Plus。实时通知采用 Server-Sent Events（SSE）技术方案，向商家、顾客、骑手推送订单状态变更通知。配送采用抢单模式，空闲骑手自主从待配送池中选择订单接单。购物车数据纯服务端 DB 存储，仅登录用户可用。每笔订单仅含一个商家的菜品，不支持跨商家下单。
```

- [ ] **Step 2: 更新 §2.1 系统结构图 — 补充通知功能**

在系统结构图中新增"通知中心"功能入口。

定位原文 F16 "购物车管理" 段落后，新增一行：
```
|          |通知中心  |
|          |·订单通知  |
|          |·配送通知  |
|          |·审核通知  |
```

- [ ] **Step 3: 更新 §2.2(2) 商家管理模块 — 补充自动接单设置**

定位原文：
```
商家可维护自己的基本信息
```

后追加：
```
（联系电话、营业时间、公告、自动接单开关等）
```

- [ ] **Step 4: 更新 §2.2(4) 订单管理模块 — 补充订单约束**

定位原文：
```
系统生成订单记录
```
后追加：
```
每笔订单仅包含同一商家的菜品，不支持跨商家合并下单
```

- [ ] **Step 5: 更新 §2.2(5) 配送管理模块 — 明确抢单模式**

定位原文：
```
系统向可用骑手推送配送任务
```

替换为：
```
系统将待配送订单推送至公共配送任务池，所有空闲骑手可查看并自主选择接单（抢单模式）
```

- [ ] **Step 6: 更新 §3.1.2 功能需求表 — 新增 S1 通知中心功能、F25（SSE待办项）、修改F16、F17**

定位原文 F16：
```
| F16 | 购物车管理 | 顾客将菜品加入购物车、修改数量、删除购物车项 |
```

替换为：
```
| F16 | 购物车管理 | 顾客（仅登录状态）将菜品加入购物车、修改数量、删除购物车项；购物车数据存服务端 DB，同一菜品重复加入时合并数量 |
```

定位原文 F17：
```
顾客确认购物车内容和配送地址后提交订单，系统生成订单记录
```

替换为：
```
顾客确认购物车内容（仅限同一商家菜品）和配送地址后提交订单，系统生成订单记录
```

在 F24 后新增两行：
```
| F25 | 实时通知推送 | 系统通过 SSE 向商家推送新订单通知、向顾客推送订单状态变更通知、向骑手推送配送任务更新通知 |
| F26 | 通知记录管理 | 用户查看历史通知列表，标记已读/全部已读，查看未读通知数量 |
```

- [ ] **Step 7: 更新 §3.1.3 完整性需求 — 补充约束**

定位原文末尾（"取消后状态变更为'已取消'。" 之后），追加：
```
- 购物车中同一顾客对同一菜品只能有一条记录（顾客ID + 菜品ID 唯一约束），多次加入合并数量；
- 顾客的默认配送地址唯一（每个顾客最多一个默认地址）；
- 商家可选择开启自动接单（下单即自动确认），开关字段在商家信息表中；
- 每笔订单仅包含一个商家的菜品（商家ID 在订单级别唯一）；
- 订单中的配送地址ID必须存在于配送地址表中（参照完整性）。
```

- [ ] **Step 8: 更新 §3.1.4 安全性需求 — 补充 SSE 认证**

定位原文倒数第二条（"接口访问需进行身份认证" 之后），追加：
```
- SSE 连接建立需通过 JWT Token 认证，服务端按用户 ID 和角色推送对应事件，保证通知送达目标的正确性；
```

- [ ] **Step 9: 提交**

```bash
git add 外卖管理系统-系统需求分析.md
git commit -m "docs(需求分析): 补充地址模型、购物车、抢单、SSE推送、通知功能等设计决定"
```

---

### Task 2: 更新系统需求分析 — 数据字典（§3.3）

**Files:**
- Modify: `外卖管理系统-系统需求分析.md`

- [ ] **Step 1: 更新 §3.3.1 数据项定义 — 新增 address、notification、operation_log 数据项**

定位原文数据项表格末尾（"配送状态" 行之后），追加：

```markdown
| 地址ID | 整型(int) | --- | ≥ 1 | 系统自动生成 |
| 是否默认地址 | 布尔型(tinyint) | 1 | 0否/1是 | 每个顾客最多一个 |
| 通知ID | 整型(int) | --- | ≥ 1 | 系统自动生成 |
| 事件类型 | 字符型(char) | 32 | order_new/order_accepted/order_ready/delivery_accepted/delivery_arrived/merchant_audited | 通知的事件类别 |
| 通知标题 | 字符型(char) | 100 | --- | |
| 通知内容 | 字符型(char) | 500 | --- | |
| 关联对象ID | 字符型(char) | 64 | --- | 订单编号/配送ID等 |
| 是否已读 | 布尔型(tinyint) | 1 | 0未读/1已读 | |
| 日志ID | 整型(int) | --- | ≥ 1 | 系统自动生成 |
| 操作类型 | 字符型(char) | 32 | LOGIN/LOGOUT/SUBMIT_ORDER/ACCEPT_ORDER/CANCEL_ORDER/CONFIRM_RECEIPT/ACCEPT_DELIVERY/UPDATE_DELIVERY_STATUS/MERCHANT_AUDIT/DISH_UPDATE/USER_STATUS_CHANGE | |
| 操作对象类型 | 字符型(char) | 32 | ORDER/DISH/MERCHANT/USER/DELIVERY | |
| 操作详情 | 字符型(char) | 500 | --- | 操作摘要描述 |
| 操作IP | 字符型(char) | 45 | --- | IPv4/IPv6 |
| 用户代理 | 字符型(char) | 255 | --- | 浏览器 User-Agent |
| 支付方式 | 字符型(char) | 16 | alipay/wechat/cash（预留） | 本期不实现 |
| 支付状态 | 字符型(char) | 8 | pending/paid/refunded（预留） | 本期不实现 |
| 支付时间 | 日期时间型 | --- | --- | 本期不实现 |
| 商家评分 | 货币型(decimal) | --- | 0.0-5.0（预留） | 本期不实现 |
| 自动接单 | 布尔型(tinyint) | 1 | 0否/1是 | |
```

- [ ] **Step 2: 更新 §3.3.1 数据项定义 — 修改顾客相关字段**

定位原文 `配送地址-省` 行，在 `配送地址-详细` 行之后删除四行地址项，追加：

删除以下四行：
```
| 配送地址-省 | ...
| 配送地址-市 | ...
| 配送地址-区 | ...
| 配送地址-详细 | ...
```

新增：
```markdown
| 默认配送地址ID | 整型(int) | --- | --- | 参照 address.id，可为空 |
```

- [ ] **Step 3: 更新 §3.3.1 数据项定义 — 修改顾客ID说明**

定位原文 `顾客ID` 行中的说明 "与用户ID关联，一一对应"，改为：
```
与用户ID关联，一一对应
```

不变。但定位 `手机号` 在顾客数据结构中的行，注意数据字典中已有一个手机号，需要确认是否冗余——实际上顾客的 phone 字段在 customer 表中存在，但数据项定义中只需保留即可，无需修改。

- [ ] **Step 4: 更新 §3.3.2 数据结构定义 — 修改 DS-2 顾客**

定位原文：
```
DS-2 顾客
> 顾客 = 顾客ID + 顾客姓名 + 手机号 + 配送地址-省 + 配送地址-市 + 配送地址-区 + 配送地址-详细
```

替换为：
```markdown
**DS-2 顾客**

> 顾客 = 顾客ID + 顾客姓名 + 手机号 + 默认配送地址ID
>
> 配送地址已独立为 DS-10 address，顾客通过默认配送地址ID 关联。
```

- [ ] **Step 5: 更新 §3.3.2 数据结构定义 — 新增 DS-10、DS-11、DS-12**

定位 DS-9 配送 之后，追加：

```markdown
**DS-10 配送地址**

> 配送地址 = 地址ID + 顾客ID + 省 + 市 + 区 + 详细地址 + 是否默认地址

**DS-11 通知**

> 通知 = 通知ID + 用户ID + 事件类型 + 通知标题 + 通知内容 + 关联对象ID + 是否已读 + 创建时间

**DS-12 操作日志**

> 操作日志 = 日志ID + 用户ID + 用户身份 + 操作类型 + 操作对象类型 + 操作对象ID + 操作详情 + 操作IP + 用户代理 + 创建时间
```

- [ ] **Step 6: 更新 §3.3.3 数据流定义 — 新增 SSE 推送数据流**

定位 DF-11 统计报表之后，追加：

```markdown
**DF-12 实时通知推送**

> 实时通知推送 = 事件类型 + 通知标题 + 通知内容 + 关联对象ID
>
> 来源：系统 SSE 推送服务 → 去向：商家/顾客/骑手
> 说明：订单状态变更、配送状态更新等事件发生时，系统通过 SSE 向相关用户实时推送通知
```

- [ ] **Step 7: 更新 §3.3.4 数据存储定义 — 修改 D2 顾客信息表**

定位 D2 顾客信息表：
```
存储结构：DS-2 顾客（见数据结构定义）
```

在其后追加：
```
注：配送地址已独立为 address 表，顾客表不再存储地址字段。顾客通过 default_address_id 关联默认地址。
```

- [ ] **Step 8: 更新 §3.3.4 数据存储定义 — 新增数据存储定义**

定位 D8 配送信息表之后，追加：

```markdown
**D9 配送地址表**

> D9 配送地址表
>
> 存储结构：DS-10 配送地址（见数据结构定义）
>
> 主键：地址ID
>
> 外键：顾客ID（参照D2）
>
> 索引：按地址ID建立主索引，按顾客ID建立普通索引
>
> 数据量：约每个顾客2-3个地址
>
> 访问频率：中频，下单时选择地址

**D10 通知记录表**

> D10 通知记录表
>
> 存储结构：DS-11 通知（见数据结构定义）
>
> 主键：通知ID
>
> 外键：用户ID（参照D1）
>
> 索引：按通知ID建立主索引，按用户ID建立普通索引，按（用户ID + 是否已读）建立联合索引
>
> 数据量：随通知推送量增长
>
> 访问频率：中频，用户查看通知列表和获取未读数

**D11 操作日志表**

> D11 操作日志表
>
> 存储结构：DS-12 操作日志（见数据结构定义）
>
> 主键：日志ID
>
> 外键：用户ID（参照D1）
>
> 索引：按日志ID建立主索引，按用户ID建立普通索引，按创建时间建立普通索引，按（操作对象类型 + 操作对象ID）建立联合索引（便于审计追踪）
>
> 数据量：每次关键操作一条记录，随时间增长
>
> 访问频率：低频，仅在审计和管理查询时访问
```

- [ ] **Step 9: 修改 §3.3.5 P1 用户管理 — 注册流程中顾客记录创建逻辑**

定位 P1 处理逻辑中：
```
e. 若身份为顾客，同步在D2中创建顾客记录；
```

在其后追加：
```
（默认配送地址ID为空，用户首次登录后自行添加地址）；
```

- [ ] **Step 10: 提交**

```bash
git add 外卖管理系统-系统需求分析.md
git commit -m "docs(需求分析): 更新数据字典，新增address/notification/operation_log等数据结构"
```

---

### Task 3: 更新概念逻辑结构设计 — E-R 图（§1）

**Files:**
- Modify: `外卖管理系统-概念逻辑结构设计-完整版.md`

- [ ] **Step 1: 更新 §1.2.1 用户管理模块局部 E-R 图 — 顾客实体移除地址属性**

定位顾客实体的 mermaid 代码块：

```mermaid
顾客 {
    int 顾客ID PK "外键，参照用户.用户ID"
    varchar 姓名 ""
    varchar 配送地址_省 ""
    varchar 配送地址_市 ""
    varchar 配送地址_区 ""
    varchar 配送地址_详细 ""
    boolean 是否默认地址 ""
}
```

替换为：
```mermaid
顾客 {
    int 顾客ID PK "外键，参照用户.用户ID"
    varchar 姓名 ""
    bigint 默认配送地址ID FK "外键，参照address.id，可为空"
}
```

同时新增 `address` 实体块：

在 顾客 实体定义之后、联系说明之前插入：
```mermaid
配送地址 {
    int 地址ID PK "系统自动生成"
    int 顾客ID FK "外键，参照顾客.顾客ID"
    varchar 省 ""
    varchar 市 ""
    varchar 区 ""
    varchar 详细地址 ""
    tinyint 是否默认地址 "0否/1是，每个顾客最多一个"
}
```

新增联系：
```mermaid
顾客 ||--o{ 配送地址 : "拥有，1:N"
```

更新联系说明表，新增一行：

```markdown
| 顾客-拥有-配送地址 | 1:N | 顾客 → 配送地址 | 一个顾客可管理多个配送地址，其中一个为默认 |
```

- [ ] **Step 2: 更新 §1.2.3 订单与配送管理模块局部 E-R 图 — 新增 address 和 notification 实体**

在配送实体之后、骑手实体之前，新增 notification 实体块：

```mermaid
通知 {
    int 通知ID PK ""
    int 用户ID FK "外键，参照用户.用户ID"
    varchar 事件类型 ""
    varchar 通知标题 ""
    varchar 通知内容 ""
    varchar 关联对象ID ""
    tinyint 是否已读 "0未读/1已读"
    datetime 创建时间 ""
}
```

修改订单实体的 address 相关属性——当前订单实体没有 address_id，需要新增。定位订单实体块，在 `订单备注` 之后追加一行属性：

```
int 地址ID FK "外键，参照配送地址.地址ID"
```

新增联系（用户与通知）：
```mermaid
用户 ||--o{ 通知 : "接收，1:N"
```

修改联系说明表，新增行：
```markdown
| 用户-接收-通知 | 1:N | 用户 → 通知 | 一个用户接收多条通知 |
```

新增 address 与 order 的联系：
修改现有的 订单-配送 联系附近，添加：
```mermaid
配送地址 ||--o{ 订单 : "配送至，1:N"
```

新增联系说明行：
```markdown
| 配送地址-配送至-订单 | 1:N | 配送地址 → 订单 | 一个地址可用于多笔订单 |
```

- [ ] **Step 3: 更新 §1.3 全局 E-R 图 — 整合所有变更**

整合以下变更到全局 E-R 图的 mermaid 代码块：

1. 顾客实体移除地址属性，新增 `默认配送地址ID`
2. 新增 `配送地址` 实体（地址ID, 顾客ID, 省, 市, 区, 详细地址, 是否默认地址）
3. 新增 `通知` 实体（通知ID, 用户ID, 事件类型, 通知标题, 通知内容, 关联对象ID, 是否已读, 创建时间）
4. 订单实体新增 `地址ID` 属性
5. 新增联系：`顾客 ||--o{ 配送地址 : "拥有，1:N"`
6. 新增联系：`配送地址 ||--o{ 订单 : "配送至，1:N"`
7. 新增联系：`用户 ||--o{ 通知 : "接收，1:N"`

同时更新联系汇总表（R16-R18）：

```markdown
| R16 | 拥有 | 1:N | 顾客 → 配送地址 | 用户管理 |
| R17 | 配送至 | 1:N | 配送地址 → 订单 | 订单与配送 |
| R18 | 接收 | 1:N | 用户 → 通知 | 订单与配送 |
```

- [ ] **Step 4: 提交**

```bash
git add 外卖管理系统-概念逻辑结构设计-完整版.md
git commit -m "docs(概念逻辑设计): 更新E-R图，新增address/notification实体，修改顾客和订单属性"
```

---

### Task 4: 更新概念逻辑结构设计 — 逻辑结构（§2）

**Files:**
- Modify: `外卖管理系统-概念逻辑结构设计-完整版.md`

- [ ] **Step 1: 更新 §2.1 初步关系模式设计 — 11→14 表**

定位关系模式表格，在 R11 配送之后追加三个关系：

```markdown
| R12 | 配送地址 | **配送地址**(地址ID, 顾客ID, 省, 市, 区, 详细地址, 是否默认地址) |
| R13 | 通知 | **通知**(通知ID, 用户ID, 事件类型, 通知标题, 通知内容, 关联对象ID, 是否已读, 创建时间) |
| R14 | 操作日志 | **操作日志**(日志ID, 用户ID, 用户身份, 操作类型, 操作对象类型, 操作对象ID, 操作详情, 操作IP, 用户代理, 创建时间) |
```

同时修改 R4 顾客关系模式（移除地址字段，新增 default_address_id）：

定位 R4：
```
| R4 | 顾客 | **顾客**(顾客ID, 姓名, 配送地址-省, 配送地址-市, 配送地址-区, 配送地址-详细) |
```

替换为：
```
| R4 | 顾客 | **顾客**(顾客ID, 姓名, 手机号, 默认配送地址ID) |
```

修改 R9 订单关系模式（新增 address_id）：

定位 R9：
```
| R9 | 订单 | **订单**(订单编号, 顾客ID, 商家ID, 下单时间, 订单状态, 总金额, 订单备注) |
```

替换为：
```
| R9 | 订单 | **订单**(订单编号, 顾客ID, 商家ID, 地址ID, 下单时间, 订单状态, 总金额, 订单备注) |
```

- [ ] **Step 2: 更新 §2.2.1 外键约束定义 — 新增关系**

改写段落首句：
```
在初步关系模式的基础上，添加外键约束以保证数据的**参照完整性**。子表的主键同时也是外键（参照用户表的用户ID），这是实现 ISA 继承关系在关系数据库中的标准做法。
```

改为：
```
在初步关系模式（共14个关系）的基础上，添加外键约束以保证数据的**参照完整性**...
```

修改 R4 顾客行，移除地址字段后的外键说明：

定位 R4：
```
| R4 | 顾客 | **顾客**(*顾客ID*, 姓名, 配送地址-省, 配送地址-市, 配送地址-区, 配送地址-详细) <br>外键：顾客ID 参照 用户(用户ID) |
```

替换为：
```
| R4 | 顾客 | **顾客**(*顾客ID*, 姓名, 手机号, *默认配送地址ID*) <br>外键：顾客ID 参照 用户(用户ID)；默认配送地址ID 参照 配送地址(地址ID) |
```

修改 R9 订单行，新增 address_id 外键：

定位 R9：
```
外键：顾客ID 参照 顾客(顾客ID)；商家ID 参照 商家(商家ID)
```

替换为：
```
外键：顾客ID 参照 顾客(顾客ID)；商家ID 参照 商家(商家ID)；地址ID 参照 配送地址(地址ID)
```

在 R11 配送之后追加三个关系：

```markdown
| R12 | 配送地址 | **配送地址**(<u>地址ID</u>, *顾客ID*, 省, 市, 区, 详细地址, 是否默认地址) <br>外键：顾客ID 参照 顾客(顾客ID) |
| R13 | 通知 | **通知**(<u>通知ID</u>, *用户ID*, 事件类型, 通知标题, 通知内容, 关联对象ID, 是否已读, 创建时间) <br>外键：用户ID 参照 用户(用户ID) |
| R14 | 操作日志 | **操作日志**(<u>日志ID</u>, *用户ID*, 用户身份, 操作类型, 操作对象类型, 操作对象ID, 操作详情, 操作IP, 用户代理, 创建时间) <br>外键：用户ID 参照 用户(用户ID) |
```

- [ ] **Step 3: 更新 §2.2.2 范式分析 — 新增 3 个关系**

定位范式分析表格末尾（配送行之后），追加：

```markdown
| **配送地址** | 地址ID → {顾客ID, 省, 市, 区, 详细地址, 是否默认地址}；无传递依赖 | BCNF |
| **通知** | 通知ID → 所有属性；无部分依赖，无传递依赖 | BCNF |
| **操作日志** | 日志ID → 所有属性；无部分依赖，无传递依赖 | BCNF |
```

- [ ] **Step 4: 更新 §2.2.3 受控冗余分析 — 新增商品自动接单标志**

定位受控冗余分析表末尾，追加：

```markdown
| 商家.自动接单 | 商家(R3) | 无（业务标志位） | 商家自行设置，下单时读取判断是否自动确认 |
```

- [ ] **Step 5: 更新 §2.3 数据库表关系图**

更新 mermaid 代码块：

1. 修改 顾客 实体块——移除省/市/区/详细地址属性，新增 `int 默认配送地址ID FK`
2. 新增 `配送地址` 实体块（地址ID, 顾客ID, 省, 市, 区, 详细地址, 是否默认地址）
3. 新增 `通知` 实体块（通知ID, 用户ID, 事件类型, 通知标题, 通知内容, 关联对象ID, 是否已读, 创建时间）
4. 新增 `操作日志` 实体块（日志ID, 用户ID, 用户身份, 操作类型, 操作对象类型, 操作对象ID, 操作详情, 操作IP, 用户代理, 创建时间）
5. 修改 `订单` 实体块——新增 `int 地址ID FK`
6. 新增联系：`顾客 ||--o{ 配送地址 : "顾客ID FK"`
7. 新增联系：`用户 ||--o{ 通知 : "用户ID FK"`
8. 新增联系：`配送地址 ||--o{ 订单 : "地址ID FK"`
9. （操作日志与用户的联系可从用户侧添加：`用户 ||--o{ 操作日志`）

更新表关系汇总表，追加 FK16-FK19：

```markdown
| FK16 | 配送地址 → 顾客 | 配送地址(顾客ID) | 顾客(顾客ID) | N:1 | 地址归属顾客 |
| FK17 | 订单 → 配送地址 | 订单(地址ID) | 配送地址(地址ID) | N:1 | 订单使用配送地址 |
| FK18 | 通知 → 用户 | 通知(用户ID) | 用户(用户ID) | N:1 | 通知发送给用户 |
| FK19 | 操作日志 → 用户 | 操作日志(用户ID) | 用户(用户ID) | N:1 | 日志关联操作者 |
```

- [ ] **Step 6: 更新 §2.4 完整性约束汇总**

定位域约束 DC10 之后，追加：

```markdown
| 域约束 | DC11 | 通知.事件类型 IN ('order_new','order_accepted','order_ready','delivery_accepted','delivery_arrived','merchant_audited') | CHECK |
| 域约束 | DC12 | 商家.自动接单 IN (0,1) | CHECK |
```

定位业务规则 BR04 之后，追加：

```markdown
| 业务规则 | BR05 | 通知与 SSE 推送同步创建 | 应用层 |
| 业务规则 | BR06 | 操作日志通过 AOP 自动记录 | 应用层 |
```

- [ ] **Step 7: 提交**

```bash
git add 外卖管理系统-概念逻辑结构设计-完整版.md
git commit -m "docs(概念逻辑设计): 更新逻辑结构，11→14关系模式，补全外键/范式/约束分析"
```

---

### Task 5: 更新项目架构文档 — SQL 建表（§5）

**Files:**
- Modify: `外卖管理系统-项目架构文档.md`

- [ ] **Step 1: 修改 customer 建表 SQL — 移除地址字段，新增 default_address_id**

定位 `CREATE TABLE customer` 块。原文中包含 address 四字段。替换整个 `customer` CREATE TABLE 语句：

```sql
-- ==================== 顾客信息表 ====================
CREATE TABLE `customer` (
    `id`              BIGINT       NOT NULL COMMENT '顾客ID（关联 user.id）',
    `name`            VARCHAR(20)  NOT NULL COMMENT '姓名',
    `phone`           CHAR(11)     NOT NULL COMMENT '手机号',
    `default_address_id` BIGINT   DEFAULT NULL COMMENT '默认配送地址ID',
    `create_time`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_default_address` (`default_address_id`),
    CONSTRAINT `fk_customer_user` FOREIGN KEY (`id`) REFERENCES `user`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='顾客信息表';
```

删除原文档中独立的 `CREATE TABLE address` 块（因为会在下一步以完整形式重新出现）。

- [ ] **Step 2: 新增 address 建表 SQL**

在 customer 表之后、merchant 表之前插入：

```sql
-- ==================== 配送地址表 ====================
CREATE TABLE `address` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '地址ID',
    `customer_id` BIGINT       NOT NULL                COMMENT '顾客ID',
    `province`    VARCHAR(20)  NOT NULL                COMMENT '省',
    `city`        VARCHAR(20)  NOT NULL                COMMENT '市',
    `district`    VARCHAR(20)  NOT NULL                COMMENT '区',
    `detail`      VARCHAR(100) NOT NULL                COMMENT '详细地址',
    `is_default`  TINYINT      NOT NULL DEFAULT 0      COMMENT '是否默认: 0否/1是',
    `create_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_customer_id` (`customer_id`),
    CONSTRAINT `fk_address_customer` FOREIGN KEY (`customer_id`) REFERENCES `customer`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配送地址表';
```

- [ ] **Step 3: 修改 merchant 建表 SQL — 新增 rating 和 auto_accept**

定位 `CREATE TABLE merchant` 块。在 `monthly_sales` 字段之后、`create_time` 之前插入：

```sql
    `rating`        DECIMAL(2,1) DEFAULT NULL             COMMENT '商家综合评分（预留）',
    `auto_accept`   TINYINT      NOT NULL DEFAULT 0       COMMENT '是否自动接单: 0否/1是',
```

- [ ] **Step 4: 修改 order_info 建表 SQL — 新增 address_id 和支付预留字段**

定位 `CREATE TABLE order_info` 块。在 `remark` 字段之后、`create_time` 之前，确认现在有 `address_id`。如果当前没有，需要在 `remark` 之后新增：

```sql
    `address_id`       BIGINT         NOT NULL                COMMENT '配送地址ID',
```

在 `address_id` 和 `create_time` 之间插入：

```sql
    `payment_method`   VARCHAR(16)    DEFAULT NULL             COMMENT '支付方式（预留）: alipay/wechat/cash',
    `payment_status`   VARCHAR(8)     DEFAULT NULL             COMMENT '支付状态（预留）: pending/paid/refunded',
    `payment_time`     DATETIME       DEFAULT NULL             COMMENT '支付时间（预留）',
```

同时新增 `address_id` 的外键：
```sql
    KEY `idx_address_id` (`address_id`),
```

在外键约束部分新增：
```sql
    CONSTRAINT `fk_order_address` FOREIGN KEY (`address_id`) REFERENCES `address`(`id`),
```

- [ ] **Step 5: 新增 cart_item 建表 SQL**

在 order_item 表之后、rider 表之前插入：

```sql
-- ==================== 购物车表 ====================
CREATE TABLE `cart_item` (
    `id`          BIGINT   NOT NULL AUTO_INCREMENT COMMENT '购物车项ID',
    `customer_id` BIGINT   NOT NULL                COMMENT '顾客ID',
    `dish_id`     BIGINT   NOT NULL                COMMENT '菜品ID',
    `quantity`    INT      NOT NULL DEFAULT 1      COMMENT '数量',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_customer_dish` (`customer_id`, `dish_id`),
    KEY `idx_customer_id` (`customer_id`),
    CONSTRAINT `fk_cart_customer` FOREIGN KEY (`customer_id`) REFERENCES `customer`(`id`),
    CONSTRAINT `fk_cart_dish` FOREIGN KEY (`dish_id`) REFERENCES `dish`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='购物车表';
```

- [ ] **Step 6: 新增 notification 建表 SQL**

在 delivery 表之后插入：

```sql
-- ==================== 通知记录表 ====================
CREATE TABLE `notification` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '通知ID',
    `user_id`     BIGINT       NOT NULL                COMMENT '接收用户ID',
    `event_type`  VARCHAR(32)  NOT NULL                COMMENT '事件类型: order_new/order_accepted/order_ready/delivery_accepted/delivery_arrived/merchant_audited',
    `title`       VARCHAR(100) NOT NULL                COMMENT '通知标题',
    `content`     VARCHAR(500) NOT NULL                COMMENT '通知内容',
    `ref_id`      VARCHAR(64)  DEFAULT NULL            COMMENT '关联对象ID（如订单编号）',
    `is_read`     TINYINT      NOT NULL DEFAULT 0      COMMENT '是否已读: 0未读/1已读',
    `create_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_user_read` (`user_id`, `is_read`),
    CONSTRAINT `fk_notification_user` FOREIGN KEY (`user_id`) REFERENCES `user`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知记录表';
```

- [ ] **Step 7: 新增 operation_log 建表 SQL**

在 notification 表之后插入：

```sql
-- ==================== 操作日志表 ====================
CREATE TABLE `operation_log` (
    `id`          BIGINT        NOT NULL AUTO_INCREMENT COMMENT '日志ID',
    `user_id`     BIGINT        DEFAULT NULL             COMMENT '操作用户ID',
    `user_role`   VARCHAR(8)    DEFAULT NULL             COMMENT '操作用户身份',
    `operation`   VARCHAR(32)   NOT NULL                 COMMENT '操作类型: LOGIN/LOGOUT/SUBMIT_ORDER/ACCEPT_ORDER/CANCEL_ORDER/CONFIRM_RECEIPT/ACCEPT_DELIVERY/UPDATE_DELIVERY_STATUS/MERCHANT_AUDIT/DISH_UPDATE/USER_STATUS_CHANGE',
    `target_type` VARCHAR(32)   DEFAULT NULL             COMMENT '操作对象类型: ORDER/DISH/MERCHANT/USER/DELIVERY',
    `target_id`   VARCHAR(64)   DEFAULT NULL             COMMENT '操作对象ID',
    `detail`      VARCHAR(500)  DEFAULT NULL             COMMENT '操作详情（摘要）',
    `ip`          VARCHAR(45)   DEFAULT NULL             COMMENT '操作IP',
    `user_agent`  VARCHAR(255)  DEFAULT NULL             COMMENT '用户代理',
    `create_time` DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_create_time` (`create_time`),
    KEY `idx_target` (`target_type`, `target_id`),
    CONSTRAINT `fk_log_user` FOREIGN KEY (`user_id`) REFERENCES `user`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

- [ ] **Step 8: 更新 §5.3 核心设计说明 — 补充新增表的设计要点**

定位 §5.3 末尾（`生产环境下若性能敏感可选择性移除外键但保留索引。`之后），追加：

```markdown
- **地址独立存储：** 配送地址从顾客表中独立为 `address` 表，支持顾客管理多个地址（家、公司等），其中一个为默认地址。下单时通过 `order_info.address_id` 快照关联。
- **通知记录持久化：** `notification` 表存储所有推送通知的历史记录，支持已读/未读状态管理。通知与 SSE 推送同步创建：推送成功则插入通知记录，用户可在通知中心回溯。
- **操作日志审计：** `operation_log` 表通过 AOP 切面自动记录关键操作（登录、下单、状态变更等），便于安全审计和问题溯源。
- **cart_item 唯一约束：** 同一顾客对同一菜品只有一条购物车记录（`uk_customer_dish`），多次加入同一菜品时合并数量而非新增记录。
- **支付预留字段：** `order_info` 新增 `payment_method`、`payment_status`、`payment_time` 三个预留字段，本期置 NULL，后续接入支付时无需改表结构。
- **商家自动接单：** `merchant.auto_accept` 标志位，为 1 时顾客提交订单后系统自动确认接单，跳过"待接单"状态直接进入"备餐中"。
```

- [ ] **Step 9: 提交**

```bash
git add 外卖管理系统-项目架构文档.md
git commit -m "docs(架构文档): 新增cart_item/address/notification/operation_log建表SQL，修改customer/merchant/order_info表结构"
```

---

### Task 6: 更新项目架构文档 — SSE + API + 文件上传（§6-§10）

**Files:**
- Modify: `外卖管理系统-项目架构文档.md`

- [ ] **Step 1: 更新 §6.1 REST API 接口清单 — 新增通知 API**

在订单模块和配送模块之间插入：

```yaml
# ==================== 通知模块 ====================
GET    /api/v1/notifications                  # 获取通知列表（分页，按时间倒序）
PUT    /api/v1/notifications/{id}/read        # 标记已读
PUT    /api/v1/notifications/read-all         # 全部标记已读
GET    /api/v1/notifications/unread-count     # 未读数量
```

- [ ] **Step 2: 更新 §6.1 REST API 接口清单 — 新增 SSE 端点**

在 API 清单末尾（统计模块之后）追加：

```yaml
# ==================== SSE 实时推送 ====================
GET    /api/v1/sse/events                     # SSE 连接端点（需 JWT 认证，按角色推送）
```

- [ ] **Step 3: 更新 §6.1 REST API 接口清单 — 新增文件上传端点**

在菜品模块末尾追加：

```yaml
POST   /api/v1/merchant/dishes/{id}/image     # 商家：上传菜品图片
```

- [ ] **Step 4: 新增 §8.X SSE 实时推送实现**

定位 §8.4 骑手接单互斥之后、§9 之前，新增：

```markdown
### 8.5 SSE 实时推送

采用 Spring Boot + SseEmitter 实现服务端单向推送。

**推送事件类型：**

| 事件名 | 触发时机 | 推送对象 |
|--------|---------|---------|
| `order:new` | 顾客提交新订单 | 对应商家 |
| `order:accepted` | 商家确认接单 | 对应顾客 |
| `order:ready` | 商家备餐完成 | 所有空闲骑手 |
| `delivery:accepted` | 骑手接配送单 | 对应顾客 |
| `delivery:arrived` | 骑手确认送达 | 对应顾客 |
| `merchant:audited` | 管理员审核商家（通过或驳回） | 对应商家 |

**连接管理实现：**

```java
@Component
public class SseConnectionManager {

    // user_id → SseEmitter
    private final ConcurrentHashMap<Long, SseEmitter> connections = new ConcurrentHashMap<>();

    public SseEmitter createConnection(Long userId, String role) {
        SseEmitter emitter = new SseEmitter(0L); // 0 = no timeout
        connections.put(userId, emitter);
        emitter.onCompletion(() -> connections.remove(userId));
        emitter.onTimeout(() -> connections.remove(userId));
        emitter.onError(e -> connections.remove(userId));
        return emitter;
    }

    public void sendEvent(Long userId, String eventName, Object data) {
        SseEmitter emitter = connections.get(userId);
        if (emitter != null) {
            try {
                emitter.send(SseEmitter.event()
                    .name(eventName)
                    .data(data, MediaType.APPLICATION_JSON));
            } catch (IOException e) {
                connections.remove(userId);
            }
        }
    }

    // 广播给所有角色用户
    public void broadcastToRole(String role, String eventName, Object data) { ... }
}
```

**Controller 端点：**

```java
@GetMapping("/api/v1/sse/events")
public SseEmitter subscribeEvents(Authentication auth) {
    Long userId = ((UserDetailsImpl) auth.getPrincipal()).getUserId();
    String role = auth.getAuthorities().iterator().next().getAuthority();
    return sseConnectionManager.createConnection(userId, role);
}
```

**在业务流程中调用（以订单提交为例）：**

```java
// submitOrder() 末尾
String[] merchantIds = getMerchantUserIds(order.getMerchantId());
for (Long merchantUserId : merchantIds) {
    sseConnectionManager.sendEvent(merchantUserId, "order:new", orderVO);
}
// 同时写入 notification 表
notificationService.create(merchantUserId, "order_new", "新订单通知",
    "您有一条新订单（" + orderNo + "），金额 ¥" + totalAmount, orderNo);
```

**前端连接（Vue 3）：**

```typescript
// composables/useSse.ts
export function useSse() {
  const eventSource = new EventSource('/api/v1/sse/events', {
    // JWT token 通过 URL 参数或 cookie 传递
  });

  eventSource.addEventListener('order:new', (e) => {
    const order = JSON.parse(e.data);
    // 通知提示 + 列表刷新
  });

  eventSource.onerror = () => {
    // 浏览器自动重连，无需手动处理
  };
}
```
```

- [ ] **Step 5: 新增 §8.X 文件上传实现**

在 SSE 章节之后新增：

```markdown
### 8.6 菜品图片上传

采用本地文件存储方案。

**存储路径规范：**

```
/uploads/dishes/{merchant_id}/{yyyyMMddHHmmss}_{uuid}.{ext}
```

**约束：**

| 项 | 值 |
|----|-----|
| 允许格式 | JPG / PNG / WebP |
| 最大文件大小 | 5 MB |
| 访问路径 | `/static/dishes/{merchant_id}/{filename}` |

**Controller 实现：**

```java
@PostMapping("/merchant/dishes/{id}/image")
@PreAuthorize("hasRole('MERCHANT')")
public Result<String> uploadImage(@PathVariable Long id,
                                   @RequestParam("file") MultipartFile file) {
    // 1. 校验文件类型
    String contentType = file.getContentType();
    if (!List.of("image/jpeg", "image/png", "image/webp").contains(contentType)) {
        throw new BusinessException("仅支持 JPG/PNG/WebP 格式");
    }
    // 2. 校验文件大小
    if (file.getSize() > 5 * 1024 * 1024) {
        throw new BusinessException("文件大小不能超过 5MB");
    }
    // 3. 生成文件名和路径
    Long merchantId = getCurrentMerchantId();
    String ext = FilenameUtils.getExtension(file.getOriginalFilename());
    String filename = LocalDateTime.now().format(FORMATTER) + "_" + UUID.randomUUID() + "." + ext;
    Path dir = Path.of("uploads", "dishes", String.valueOf(merchantId));
    Files.createDirectories(dir);
    // 4. 保存文件
    Path target = dir.resolve(filename);
    file.transferTo(target);
    // 5. 更新菜品 image_url
    String imageUrl = "/static/dishes/" + merchantId + "/" + filename;
    dishService.updateImageUrl(id, imageUrl);
    return Result.ok(imageUrl);
}
```
```

- [ ] **Step 6: 更新 §10 开发规范 — 补充表数量**

定位文档末尾 `<文档维护说明>` 区域，更新后续需补充内容：

原文：
```
后续需补充的内容：支付模块（预留）、评价系统（预留）、WebSocket 实时推送方案、Docker Compose 编排文件。
```

替换为：
```
后续需补充的内容：支付模块（预留字段已添加）、评价系统（预留）、Docker Compose 编排文件、数据库备份策略。
```

同时在附录 A 表格中确认建表数量更新（9 张 → 14 张）。

- [ ] **Step 7: 提交**

```bash
git add 外卖管理系统-项目架构文档.md
git commit -m "docs(架构文档): 新增SSE推送、文件上传方案，补充通知/文件上传API端点"
```

---

### Task 7: 重新生成 diagrams/ 下所有图表

**Files:**
- Modify: `diagrams/01-用户管理模块局部ER图.mmd`
- Modify: `diagrams/02-商家与菜品管理模块局部ER图.mmd`
- Modify: `diagrams/03-订单与配送管理模块局部ER图.mmd`
- Modify: `diagrams/04-全局ER图.mmd`
- Modify: `diagrams/05-数据库表关系图.mmd`
- Regenerate: `diagrams/*.png`, `diagrams/*.svg`, `diagrams/*.drawio`

- [ ] **Step 1: 更新 01-用户管理模块局部ER图.mmd — 新增 address 实体**

定位 `diagrams/01-用户管理模块局部ER图.mmd`。与 §1.2.1 中的 mermaid 代码块保持同步：

1. 顾客实体移除地址属性，新增 `bigint 默认配送地址ID FK`
2. 新增 `配送地址` 实体块
3. 新增 `顾客 ||--o{ 配送地址 : "拥有，1:N"` 联系

（具体 mermaid 代码同 Task 3 Step 1 中的代码块，此处不再重复。）

- [ ] **Step 2: 更新 03-订单与配送管理模块局部ER图.mmd — 新增 notification 实体、订单新增 address_id**

1. 新增 `通知` 实体块
2. 订单实体新增 `int 地址ID FK` 属性
3. 新增 `配送地址` 实体块
4. 新增联系：`配送地址 ||--o{ 订单 : "配送至，1:N"`
5. 新增联系：`用户 ||--o{ 通知 : "接收，1:N"`（若用户实体不在本图中，则标注说明）

- [ ] **Step 3: 更新 04-全局ER图.mmd — 整合所有变更**

与 Task 3 Step 3 中的全局 E-R 图保持一致：
1. 修改顾客实体
2. 新增配送地址、通知实体
3. 订单实体新增地址ID
4. 新增 3 条联系

- [ ] **Step 4: 更新 05-数据库表关系图.mmd — 整合新增表**

与 Task 4 Step 5 中的数据库表关系图保持一致：
1. 顾客、订单实体更新
2. 新增配送地址、通知、操作日志 3 个实体框
3. 新增 4 条外键联系（FK16-FK19）

- [ ] **Step 5: 运行 mmdc 重新生成 PNG 和 SVG**

```bash
cd diagrams

# 生成 01
mmdc -i 01-用户管理模块局部ER图.mmd -o 01-用户管理模块局部ER图.png -b white -s 2
mmdc -i 01-用户管理模块局部ER图.mmd -o 01-用户管理模块局部ER图.svg -b white -s 2

# 生成 02（若未修改也重新生成确保一致性）
mmdc -i 02-商家与菜品管理模块局部ER图.mmd -o 02-商家与菜品管理模块局部ER图.png -b white -s 2
mmdc -i 02-商家与菜品管理模块局部ER图.mmd -o 02-商家与菜品管理模块局部ER图.svg -b white -s 2

# 生成 03
mmdc -i 03-订单与配送管理模块局部ER图.mmd -o 03-订单与配送管理模块局部ER图.png -b white -s 2
mmdc -i 03-订单与配送管理模块局部ER图.mmd -o 03-订单与配送管理模块局部ER图.svg -b white -s 2

# 生成 04
mmdc -i 04-全局ER图.mmd -o 04-全局ER图.png -b white -s 2
mmdc -i 04-全局ER图.mmd -o 04-全局ER图.svg -b white -s 2

# 生成 05
mmdc -i 05-数据库表关系图.mmd -o 05-数据库表关系图.png -b white -s 2
mmdc -i 05-数据库表关系图.mmd -o 05-数据库表关系图.svg -b white -s 2
```

- [ ] **Step 6: 运行 gen_er_diagrams.py 重新生成 .drawio 文件**

```bash
python gen_er_diagrams.py
```

验证：检查 `diagrams/` 目录下所有 .drawio 文件都已更新。

- [ ] **Step 7: 提交**

```bash
git add diagrams/
git commit -m "docs(diagrams): 重新生成所有ER图，新增address/notification/operation_log实体"
```

---

## Self-Review

**1. Spec coverage:**

| Spec 章节 | 对应任务 |
|-----------|----------|
| §2.1 地址模型不统一 | Task 1 Step 1, Task 2 Step 2-4, Task 3 Step 1-3, Task 4 Step 1-2, Task 5 Step 1-2, Task 7 Step 1-4 |
| §3.1 SSE 推送方案 | Task 1 Step 1, Task 6 Step 1-4 |
| §3.2 通知记录表 | Task 1 Step 6, Task 2 Step 1/5, Task 3 Step 2-3, Task 4 Step 1-4, Task 5 Step 6 |
| §3.3 操作日志表 | Task 2 Step 1/5, Task 4 Step 1-4, Task 5 Step 7 |
| §3.4 支付预留字段 | Task 2 Step 1, Task 5 Step 4 |
| §3.5 菜品图片上传 | Task 6 Step 5 |
| §3.6 购物车建表 | Task 5 Step 5 |
| §4 设计决定固化 | Task 1 Step 1-5 |
| §6 文档更新清单 | 全部 Task 1-7 |

覆盖全部 spec 要求。

**2. Placeholder scan:** 无 TBD/TODO/占位符。所有步骤均有具体代码和内容。

**3. Type consistency:** 
- `address.id` 在整个计划中保持一致（BIGINT AUTO_INCREMENT）
- `notification.event_type` 取值一致
- `operation_log.operation` 取值一致
- 外键命名风格一致（`fk_<table>_<column>`）
